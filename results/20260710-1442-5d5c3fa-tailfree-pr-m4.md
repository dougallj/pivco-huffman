# tail-free-pr — M4 A/B vs main (2026-07-10)

`tail-free-pr` @ 5d5c3fa (tail-free kernels + EXACT caller contract:
dst gets exactly N bytes, no read passes in + in_len, wire unchanged)
vs `main` @ 695c36e.  Two interleaved `bench 20 --all` pairs, best-of-2.

**Decode pivco_bu geomean +3.5 % vs main** — the safety layer costs
nothing measurable vs the padded `tail-free` experiment (+1.3 % vs its
single reference run, i.e. noise).  Gains: proba50 +9.8 %, bell_s10
+8.8 %, csv_numeric +7.7 %, proba80 +6.7 %, html_wiki +6.6 %,
log_apache +6.0 %; flat-dominated rows at parity (uniform −1.3 %,
sparse_4 −1.4 %, gzip_random −0.3 % — inside those rows' run noise).

The first cut of the safety layer cost −48 % on flat roots by bouncing
whole end-of-stream regions; fixed by the exact 2*ceil(K/16) bitmap
read bound (no bounce at production sizes) and the 16-code-aligned
prefix/tail split for flat regions (<= 48-byte copy).  Raw captures:
m4-*-tailfree-pr-5d5c3fa.txt / m4-*-mainbase2-695c36e.txt.
